Source: naan
Section: utils
Priority: optional
Maintainer: Lenik <naan@bodz.net>
Build-Depends: debhelper-compat (= 13), meson, ninja-build, pkgconf,
               libssl-dev, libbas-c-dev, gettext, asciidoctor
Standards-Version: 4.6.0
Homepage: https://github.com/lenik/naan
Rules-Requires-Root: no

Package: naan
Architecture: any
Depends:
    ${shlibs:Depends},
    ${misc:Depends}
Description: compute a number from a name
 naan hashes each name and prints one decimal number:
 (digest(NAME) mod DIV) + BIAS. Profiles set the hash and DIV/BIAS
 (b/w/dw use SHA-256; wm uses SHA-1 in 2000 through 3999).
